# =============================================================================
# resources/orchestration.job.yml
# -----------------------------------------------------------------------------
# Daily orchestration Workflow: ingest CRM + ERP into bronze, then run the
# silver/gold transformation pipeline. Pipeline tasks reference the pipeline
# resources by id so the bundle wires DAG dependencies across files.
#
# In dev (mode: development) the bundle automatically PAUSES the schedule, so
# the QUARTZ_CRON below only fires in qa/prod.
# =============================================================================
resources:
  jobs:
    job_orchestration_daily:
      name: "[${bundle.target}] job_orchestration_daily"
      description: >-
        Daily batch orchestration: ERP + reference file ingestion → bronze
        (erp_ingestion pipeline), then silver + gold build (transformation
        pipeline). CRM bronze is refreshed independently on-demand via the
        Postgres pull. Schedule 05:00 America/New_York (paused in dev).

      tags:
        platform: commercial-data-platform
        environment: ${bundle.target}
        domain: orchestration

      schedule:
        quartz_cron_expression: "0 0 5 * * ?"   # 05:00 daily
        timezone_id: America/New_York
        pause_status: UNPAUSED                   # dev mode overrides this to PAUSED


      tasks:
        # NOTE: CRM bronze is now sourced from local Postgres ON-DEMAND via the
        # crm_postgres_ingestion pipeline (scripts/pull_crm_from_pg.sh), NOT this
        # job. This daily job covers ERP + reference (file-based) -> transformation;
        # bronze_crm_* must be refreshed via the Postgres pull independently.

        # 1) ERP + reference ingestion -> bronze
        - task_key: erp_ingestion
          pipeline_task:
            pipeline_id: ${resources.pipelines.erp_ingestion.id}
            full_refresh: false

        # 2) Silver + gold transformation (reads bronze_crm_* [Postgres] + bronze_erp_*/ref_*)
        - task_key: transformation
          depends_on:
            - task_key: erp_ingestion
          pipeline_task:
            pipeline_id: ${resources.pipelines.transformation.id}
            full_refresh: false

      queue:
        enabled: true
